{
  "name": "gitbook-boilerplate",
  "version": "1.0.0",
  "private": true,
  "description": "A boilerplate of Gitbook",
  "main": "index.js",
  "scripts": {
    "init": "gitbook init",
    "start": "gitbook serve",
    "build": "gitbook build",
    "publish": "gh-pages -d _book -m 'Auto publish book'",
    "deploy": "npm run build && npm run publish"
  },
  "devDependencies": {
    "gh-pages": "^0.11.0",
    "gitbook-cli": "^2.3.0"
  }
}
